#include <stdio.h>

int main() {
    // This is a comment

    /*
       This is a multi-line comment,
       it spans multiple lines
    */

   printf("Comments are ignored by the compiler and can be used to explain code or temporarily disable code for debugging\nComments can be written in-line/single line using 2 slashes (//)\nOr as a multiline comment via an opening and closing comment (/*, */)\n");

    return 0;
}